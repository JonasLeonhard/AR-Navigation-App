using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scrMarker : MonoBehaviour {


    public int augmentedImageDbIndex; //which image (index) off the Augmented image DB is associated with this marker instance. (0-based)
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
